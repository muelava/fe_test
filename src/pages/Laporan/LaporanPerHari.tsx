import { Container } from '@mantine/core';
import LalinList from '../../components/LalinList';

const LaporanPerHari = () => {
    return (
        <Container>
            <LalinList />
        </Container>
    );
};

export default LaporanPerHari;
